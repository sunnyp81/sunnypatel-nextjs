# SunnyPatel.co.uk

## Stack
- Next.js 16 (App Router) + TypeScript
- Tailwind CSS v4 (CSS variables)
- Keystatic CMS (local content, flat-file)
- shadcn/ui components
- Framer Motion (via `motion/react`)

## Deployment
- Vercel (production)
- Domain: sunnypatel.co.uk

## Content Collections (Keystatic)
- `src/content/blog/` — Blog posts (Markdoc)
- `src/content/services/` — Service pages (Markdoc)
- `src/content/portfolio/` — Case study pages (Markdoc)
- `src/content/pages/` — Static pages

## Key Paths
- `src/app/` — Next.js App Router pages
- `src/components/` — React components (sections/, about/, portfolio/, ui/)
- `src/lib/` — Utilities (content.ts, metadata.ts, schema.ts, render-markdoc.ts)
- `public/` — Static assets, llms.txt, llms-full.txt
- `keystatic.config.ts` — Collection definitions

## Schema Markup
- `src/lib/schema.ts` — All JSON-LD schema builder functions
- Global: Person + Organization + LocalBusiness (in layout.tsx)
- Blog: BlogPosting + BreadcrumbList
- Services: ProfessionalService + BreadcrumbList
- Portfolio: CreativeWork + BreadcrumbList
- About: FAQPage

## Design Tokens
- Blue accent: #5B8AEF
- Gold accent: #d79f1e
- Dark theme (class="dark" on html)
- Heading font: Space Grotesk (--font-heading)
- Body font: Inter (--font-body)

## Content Briefs
External: `G:\My Drive\SunnyPatel.co.uk\content-briefs\`

## Conventions
- All content must pass `/semantic-audit` before publishing
- Use `buildMetadata()` from `src/lib/metadata.ts` for page meta
- Use `schemaGraph()` from `src/lib/schema.ts` for JSON-LD output
- FAQ data exported from `src/components/about/about-faq.tsx`

---

# sunnypatel-nextjs — Project Brain

Per-repo brain, migrated from central claude-memory 2026-06-20. Canonical project memory now lives here.

## Current state

Sunny Patel's personal SEO + AI-search (GEO) consultant site, **sunnypatel.co.uk**. Lead-generation money site (no direct revenue stream yet, £0; goal is qualified leads / the £495 fixed-fee audit / discovery calls).

- **Live** on production. Ranks well (**position 1 for "seo consultant reading", ~1,200 impr/day**) but converts almost nothing (1-2 clicks/day, near-zero leads). The whole job now is conversion, not ranking.
- **Stack:** Next.js 16 (App Router) + TypeScript, Tailwind v4, Keystatic CMS (flat-file Markdoc), shadcn/ui, Framer Motion. Branch `master`, GitHub `sunnyp81/sunnypatel-nextjs`.
- **Deploy:** GitHub -> Vercel. NOT auto-deploy — `git push` then `npx vercel --prod`. Local build needs dummy env vars `KEYSTATIC_GITHUB_CLIENT_ID` / `KEYSTATIC_GITHUB_CLIENT_SECRET` / `KEYSTATIC_SECRET`.
- **GA4:** `G-SJRTDNRZG6`.
- **Key URLs:** `/cv` (serves `public/cv.pdf` via `next.config.ts` rewrite), `/blog/best-aeo-agencies/` (GEO flagship, 92/100 audit), the £495 audit page, `/tools/` hub (20 free SEO tools, Website Grader is the lead funnel), `/website-design/` cluster (14 pages), `/services/seo-consultant-reading/` + `/services/seo-berkshire/` (core money pages), `/ai-visibility-results/` (new Aug 26, canonical real-data AI-referral proof page, Dataset schema, linked from homepage/`/about/`/the 3 AI-visibility sales pages/3 top blog posts).

## Key facts & warnings

- 🔴 **GSC for sunnypatel.co.uk is NOT under the `sunnypat81` account** — inspect/submit fails there. Use the `2012infinite` GSC account (token-2012infinite.json).
- 🔴 **NEVER run a Cloudways live->staging DB sync** (portfolio-wide rule from Hummingbird; not repo-specific but durable).
- Deploy is manual (`git push` + `npx vercel --prod`), not auto-deploy — easy to forget the Vercel step.
- `trailingSlash: true` in `next.config.ts`. ALL internal links must use trailing-slash URLs — non-trailing links caused Google to index both variants and split ranking signals (recurring root cause, fixed repeatedly). Sitemap must emit only slash versions.
- Brand: light mode, Bricolage Grotesque (headings) + Hanken Grotesque (body), single blue accent, "SP" monogram. **No em dashes or en dashes anywhere** — commas/colons/hyphens only.
- Organic ceiling reality: "seo consultant reading" ~200 searches/mo, so even at #1 the lead ceiling is ~1.8/mo. Service-business growth needs cold outreach + directories + reviews, not just organic.
- AEO ceiling is now **off-page** (~85% of AI brand mentions are third-party) — needs brand-consensus / entity-authority outreach (Sunny has parked outreach).
- Lead source: **2 "Free SEO Audit" bookings via the Trafft widget (Jun 8)**. Trafft admin emails include the customer name and email as of Aug 13, 2026 (earlier note claiming they were stripped was wrong).
- 🔴 Aug 20: site no longer links to Trafft booking anywhere (see Aug 19-20 history entry) — the two lines above (Trafft widget, Trafft notification template) describe the pre-removal state, kept for history only.
- Manual items Sunny owes: mark GA4 `generate_lead` as a key event; paste GA4 id into Trafft Integrations (moot now booking is removed, skip unless Trafft account is kept for other reasons); Bark/Yell sameAs (GBP live, cid 2292818604572155987, but **Bark/Yell/DesignRush sameAs lines in `src/lib/schema.ts` are still commented out as of Sep 10, never actually completed despite the Jul-10 checklist saying so, corrected a stale note here that claimed otherwise; see docs/gbp-setup-checklist.md for the reviews push + NAP name decision); Wikidata entry (confirmed hard: needs an external notability anchor, e.g. a Companies House record or press coverage, not just self-submission); Clutch reviews (profile is real and live at clutch.co/profile/sunny-patel, verified Sep 10, still 0 reviews); Trustpilot profile + review-request message (prepped by Claude Sep 10, deferred to a later session, not yet created). **Bing Places for Business added Sep 10** (Store code `07550193713958698401`, NAP matches GBP exactly): status "Pending publish", 7-12 day ETA — once it publishes, add the public listing URL to the `sameAs` array in `src/lib/schema.ts` (same pattern as Clutch/GBP/LinkedIn). Lemlist/cold-outreach creds are pointers only (e.g. reference_lemlist_api_credentials in the vault) — no secrets stored here.
- CV update: overwrite `public/cv.pdf`, push `master`, `npx vercel --prod`. Editable master is the gdoc in `G:\My Drive\Sunny Patel CV\`.
- Content briefs live external: `G:\My Drive\SunnyPatel.co.uk\content-briefs\`.

## History

- **Sep 11** - Backlink-target refresh, Opus-planned via Ahrefs MCP, Sonnet-executed (8 parallel agents), live (`d9f04d6`). Sunny supplied two Ahrefs backlink CSV exports; identified 9 real link-earning pages by referring domains (seo-statistics-uk, local-seo-statistics, content-marketing-statistics, website-redesign-seo-checklist, how-much-does-an-seo-consultant-charge-uk, seo-due-diligence-investors, seo-for-multiple-websites, ai-search-optimisation service, content-briefs service), pulled anchor text/context per backlink, then had Opus (not Sonnet) do the strategic planning with live Ahrefs data before any edit, per Sunny's explicit instruction to use a higher model for planning. **Key Opus finding**: `/blog/local-seo-statistics/` looked broken (position 8.77, 1,052 impressions, 0 clicks) but the standard "refresh the stats" diagnosis was wrong, the page had already been properly rewritten 26 Aug; GSC query-level data showed 60-70% of impressions were tool-intent queries ("site stats", "website stats traffic") an article can never win, plus ~72 impressions from stale 2016/2017 archival-query association, and Ahrefs confirmed no AI Overview on that SERP so that wasn't the cause either; fix was retargeting the title/meta at the query that actually has volume ("local search stats") and adding a passage that explicitly breaks the archival association, not adding more statistics. **778% figure found repo-wide**: the "AI referral traffic grew 778% YoY" figure corrected on `ai-search-statistics` on Sep 10 had never been propagated; found and fixed in 4 more files (`best-aeo-agencies`, `brand-not-appearing-in-chatgpt` including its FAQ and a baked-in SVG chart number, `seo-consultant-reading`) plus one more instance an orchestrator pass caught that the build agents correctly left out of their stated scope. **Citation drift**: `content-marketing-statistics` had 3 external sites citing figures (82% adoption, 1,000% traffic multiplier, $22 ROI) that a prior rewrite had removed without a trace; added a "figures you may have seen elsewhere" passage naming each old number, why it's wrong, and the current sourced replacement, so the inbound citations still resolve to real content instead of a page that silently dropped the number. **3 new pages built** off Opus's Ahrefs gap analysis, each cannibalization-checked against the existing 130+ page site first: `/blog/how-many-websites-are-there/` (GB search volume 250, precedent: rebootonline's equivalent page holds 173 referring domains, their single biggest link asset), `/blog/uk-ecommerce-seo-statistics/` (feeds 4 existing commercial ecommerce service pages with no informational asset previously backing them), `/blog/uk-dental-marketing-statistics/` (no measured search volume, stated honestly on the page; built as a sales/credibility asset for the live Envisage Dental proposal, not an SEO play). Rejected candidates (SEO ROI stats, GBP stats, PPC stats, ChatGPT stats, voice search stats, SEO pricing benchmarks, UK SEO market size) all logged with the specific existing page each would have cannibalized. **QA**: same 2-pass pattern as Sep 10 (dash/watermark, YAML, tsc, build, 15 URLs live-checked, design validator, Codex proofing) but the QA agent itself stalled twice waiting on its own detached Codex process before finally applying results, both times because the agent's "wait until file exists" loop wasn't paired with an actual wait mechanism, had to be resumed via SendMessage each time. Codex's second pass on this batch found 71 real issues (47 applied): two arithmetic errors (an 81%-of-10.3M contradiction on the new websites-statistics page, a 5-10%-of-8,000 miscalculation on the checklist page), a CTR position-ranking error on seo-statistics-uk (claimed position 8 had the lowest CTR in the top 10, position 2 actually did per the page's own table), and a live "I run 45 of my own sites" contradicting the site-wide 44 convention on the very same local-seo-statistics page being fixed for a different reason. **Flagged for Sunny, not fixed (deliberately, no fabrication)**: (1) `seo-due-diligence-investors` has a DR32 dofollow backlink from linkbuildingjournal.co.uk citing a "10-30% post-acquisition traffic drop" claim that was never on the page, no primary source could be verified, needs Sunny's call; (2) `/services/content-briefs/` and `/blog/what-is-a-content-brief/` have near-identical "what's included" component lists, real duplication, ownership decision needed; (3) 8 pre-existing "45-site" references survive outside this changeset in portfolio pages (`portfolio/niche-affiliate-seo-portfolio-45-sites/` and others) contradicting the site-wide "44" figure, predates this session, out of scope here. Workflow note: this is the first session where Opus (not Fable) did the planning layer while Sonnet executed and Fable was not invoked at all, per Sunny's explicit "use higher models for planning" instruction; matches the `feedback_model-selection-per-task` rule (deliberate per-task model choice) rather than the older Fable-gates/Sonnet-executes split.
- **Sep 10 (evening)** - Winners improvement pass, Fable-directed, Sonnet-executed, live (`5abefea`). Targets chosen from 90-day GSC: `/blog/ai-search-statistics/` (25 clicks, 6,930 impr, pos 13), `/tools/schema-generator/` (19 clicks, 3,700 impr, pos 16), `/blog/top-geo-agencies/` (9 clicks, 8,093 impr, pos 20, much of it AI-assistant agent prompts researching UK GEO agencies). Ownership check first: `best-aeo-agencies` owns "aeo agency*" queries, `top-geo-agencies` owns "geo agency*", clean split, both kept. **(1) Stats post**: every figure re-verified against primary sources, now "120 AI Search Statistics 2026", six new H2s targeting real query gaps (market share vs AI, searches per day, most cited sites, US/UK studies incl. Ofcom/ONS, 2026 trends, tracking), three SVG charts redrawn; the old 778% Similarweb figure is unconfirmable and was replaced by the verified 357%/1.13B (June 2025), and Seer's CTR stat is a June 2024 to Sept 2025 time comparison on AIO queries (1.76% to 0.61%, Seer states 61%), not a same-page with/without split; a Codex pass "corrected" it to 65% by arithmetic and was reverted to the source's figure. **(2) Schema generator**: 6 to 16 types, JSON-LD/Microdata toggle, download + validator buttons, 16 per-type routes `/tools/schema-generator/<slug>/` with own metadata/FAQ (technicalseo.com pattern), in sitemap. Verified against Google's changelog: FAQ rich results removed 7 May 2026 (docs deleted 15 Jun 2026), HowTo removed 2023, sitelinks search box removed Nov 2024, self-serving Review snippets blocked; the pages state this plainly. **(3) GEO listicle**: 7 to 12 agencies each verified via live service page (added Semetrical, Screaming Frog, Passion Digital, Add People, Hallam), region table (no verifiable Exeter/Leicester/Edinburgh agency, said so rather than invent), sector section, pricing table, specialist vs agency, ranking method with "Last verified" date, ItemList schema updated. **QA**: dash/watermark scan, YAML, tsc, next build, 20 URLs 200, design validator 0 errors, Codex proofing (15 fixes applied, rejected subjective ones). **Gotchas**: the first QA agent stalled for an hour with no output and was killed; `taskkill /F /IM node.exe` in a QA agent also kills every MCP server in the session, kill by PID instead. Brainstorm team (4 Sonnet lenses + Opus synthesis) produced a 30-day links/clicks/leads plan at `G:\My Drive\SEO\sites\sunnypatel.co.uk\growth\links-clicks-leads-plan-sep10.md`; top item is an AI Visibility Snapshot lead magnet, which must build on the existing `/tools/ai-visibility-checker/` rather than a new route, and item 2 (restore a booking CTA) needs Sunny's call since the Trafft link was removed on purpose Aug 20.
- **Sep 10 (continued)** — Site-wide improvement pass, Fable-directed, real-data-verified, 3 commits deployed (`a4e3fba`, `179b976`, `768a116`). Fable (via a background agent) analysed a fresh Ahrefs export + a 90-day GSC pull and proposed an internal-link plan plus site improvements; every recommendation was re-verified against live GSC/SERP data before executing, and two of Fable's premises turned out wrong once checked. **(1) Confirmed & fixed**: homepage nav/footer/CTA anchor "SEO Reading" pointed at `/` instead of `/services/seo-consultant-reading/`, and a live WebSearch for "seo consultant reading" showed the homepage and the Reading page both ranking right now, direct confirmation of real cannibalization (same problem the May-10 entry below says was fixed once already). Fixed the anchors + found a second instance in `best-local-seo-agencies` ("London" linking to `/`). **This is not fully solved** — the anchor/link fixes address internal PageRank flow, not why Google's own ranking algorithm still prefers the homepage for this query; a real fix likely needs de-optimising the homepage's implicit Reading relevance (schema/content/entity signals), flagged as its own follow-up. **(2) Rejected**: Fable's plan to merge `paid-seo-audit` into `technical-seo-audit` (301 redirect) was based on Ahrefs showing technical-seo-audit's traffic dropping "13 to 0" — pulled real GSC daily data and found the page actually gets 100-250 impressions/day with improving position (35->20 over 3 months); the Ahrefs number was noise (this site's Ahrefs traffic estimates are already known-unreliable, see its own `/blog/google-ctr-study/`). Kept both pages (technical-seo-audit ranks broadly and funnels its own CTAs to paid-seo-audit's bespoke sales page, a legitimate pillar-to-conversion pattern already in place); only fixed technical-seo-audit's redundant meta title ("SEO Audit" repeated 3x) and an over-length meta description. **(3) Confirmed & executed**: `local-seo-reading` (9 impr/90d), `digital-marketing-reading` (225 impr, 0 clicks), `seo-strategy-reading` (0 impressions) were genuinely dead, unlike the audit page — folded local-seo-reading's real unique content (GBP ranking-factor stats, a distinct local-pack case study) into a new section on `seo-consultant-reading`, 301'd all three, removed their content-collection entries and orphaned FAQ/schema dict entries. **(4) CTR rewrite** on `seo-consultant-reading`, `seo-berkshire`, `technical-seo-audit` after checking live SERPs: `seo-berkshire`'s title didn't exact-match its actual top query ("seo berkshire", position 7.6, still 0 clicks) and Google was already silently rewriting the SERP snippet around it, retitled to match; `seo-consultant-reading` got a real differentiator (direct access, no agency markup) added since position-22 CTR is expected to be near-zero regardless of title per the site's own published research, don't expect this alone to move clicks much; the real lever there is the cannibalization in (1) and the ranking position itself. **(5) Other real bugs found+fixed**: `/services/generative-engine-optimisation/` was a dead redirect hop still linked from 4 files (repointed to `/services/ai-search-optimisation/` directly); `/Services` (capitalised) 404'd instead of redirecting (added case-specific redirect); "45 vs 40+" portfolio-count inconsistency across 3 About components (fixed to 45; left "44-site"/"53 UK sites" mentions alone where tied to a specific dated study's own sample size, e.g. `google-ctr-study`, or a specific measured citation-count claim, since changing those would misrepresent what was actually measured). **(6) Not investigated further, real bug, needs its own session**: `/services/ai-search-optimisation/` shows "Discovered - currently not indexed" in GSC despite 15+ real internal links from high-DR posts — Google is choosing not to crawl it, likely due to semantic overlap with the site's 5 other AI-visibility pages (`/ai-visibility/`, `/ai-visibility-consultant/`, `/geo-agency/`, `/is-your-brand-visible-in-ai-search/`); needs an ownership decision (pick one canonical AI-visibility page) before any fix, same class of problem as (1) and (2), not attempted this session. Workflow note: Fable (model override `fable` on the Agent tool) did direction/analysis only, no code; Sonnet executed and re-verified every data claim against live GSC/SERP before acting on it, per the site's own "Ahrefs is unreliable here" lesson from earlier in this same session.
- **Sep 10** — Hero animation restored + 2 new homepage sections, deployed live (`2c516f3`). Root cause of "animation stopped": `54dcc14` (Aug 31) stripped framer-motion from `shape-landing-hero.tsx` thinking it was a perf win, but framer-motion stays loaded site-wide anyway (14 other components use it) so it saved nothing — restored the drop-in + infinite-float entry animation with `useReducedMotion` respected. Also found the Sep 4 abandoned homepage-redesign WIP (`src/app/page.tsx` locally modified + untracked `src/components/homepage-redesign/`, flagged but left untouched in the Sep 4-5 entry below) was never claimed — inspected it live via dev server, confirmed it duplicated proof/stats/quick-form content the live homepage already has, so discarded the WIP files and `docs/semantic-audit-results.tsv` changes rather than merge it. Extracted its two genuinely new pieces as proper components matching the live dark theme (not the WIP's own light-theme CSS module): `search-model.tsx` (the "Findable/Understood/Trusted/Chosen" 4-step framework) and `live-proof.tsx` (real GSC weekly-clicks chart + keyword-position list from `proof.json`), inserted between `About` and `Portfolio`. **Real bug found and fixed**: the chart's per-point `<circle><title>{value}</title></circle>` tooltips caused a hydration mismatch — React 19's automatic `<title>`-element resource-hoisting only lets through 2 titles per page (the document `<title>` plus one more), server-renders every additional repeated `<title>` empty, client hydrates them with content. Confirmed via curl on SSR output (`8` empty `<title></title>` vs `2` populated) before fixing — don't use bare SVG `<title>` for repeated per-element tooltips in this stack, use visible `<text>` labels or `aria-label` instead. Deployed via `npx vercel --prod` (local CLI was logged out, `vercel login` device-flow completed automatically via an existing browser session), live-verified via curl + rendered screenshot on production.
- **Sep 4-5** — 9 new service pages shipped from a real Ahrefs keyword-gap analysis, using a Fable-directs/Codex-builds/Fable-reviews pipeline established this session. (1) Pulled a 4,310-row Ahrefs GB "seo consultant" keyword export, cross-referenced against the site's 57 existing service slugs by a `codex exec` pass: found 9 genuine service-line gaps (real volume, near-zero KD, no existing page) — deliberately NOT more location/city pages, since the Jul19 consolidation bet already proved that pattern doesn't work on this site (see Sep 3 entry below and `docs/evolve/active-hypotheses.md` constraint on GBP/reviews being the real lever). (2) Built `seo-migration-consultant` first as a single proof page (real credentials only: 44-site portfolio, WordPress/Shopify/Astro migration experience, no invented clients), shipped + verified, with 2 new internal links into `/blog/top-geo-agencies/` from `ai-search-statistics` and `ai-search-optimisation` (that page had only 2 inbound links vs ~9 and ~20 for the site's other strong pages). (3) Sunny explicitly overrode the "wait 6 weeks and prove it first" caution and said build all 8 remaining gaps now: `travel-seo-consultant`, `architects-seo-consultant`, `healthcare-seo-consultant`, `hotel-seo-consultant`, `wix-seo-consultant`, `automotive-seo-consultant`, `seo-training-consultant`, `squarespace-seo-consultant`. Fable directed a batch brief (H1/meta/H2 outline/FAQs/links per page), Codex built all 8 + self-reviewed (94-97/100), Fable ran an adversarial final QA pass and caught a repeated "I don't invent clients" disclaimer tic across 4 pages (deleted, since a prospect reading "I don't make up clients" wonders why you needed to say it), a GMC-only regulator reference on the healthcare page that should cover GDC/HCPC/NMC too (fixed), and an OTA/accommodation-groups wording mismatch on travel (fixed). Healthcare page is YMYL-reviewed: zero clinical/treatment/outcome claims, all language scoped to marketing/visibility, clinical authorship stays explicitly with the provider. (4) `seo-training-consultant` was deliberately held back from that commit because its workshop pricing was Fable's draft, not Sunny's — Sunny then said "codex to decide and ship," so Codex independently priced it (half-day £750, full-day £1,250, six-week programme £2,500, down from a draft £3,500) benchmarked against this site's own £200/session consulting rate, and it shipped as a 9th page. (5) **Two real deploy bugs found and fixed live, both worth remembering**: first, `vercel --prod` deploys the local working directory, not git state — excluding a file from the commit does NOT stop it going live if it's still sitting on local disk; the training page briefly went live with unconfirmed pricing on the first deploy before this was caught and fixed (moved out of the content collection, rebuilt, redeployed). Second, `vercel --prod` run from a `git worktree` silently fails to build (shows "Building..." forever, deployment record shows 0ms build, status UNKNOWN) — worktrees use a `.git` file pointer instead of a real `.git` directory and Vercel CLI doesn't handle that; a real `git clone` works fine. This mattered because a concurrent session was mid-flight on an unrelated homepage redesign (`src/app/page.tsx`, `src/components/homepage-redesign/`, tracked via 3 real audit-log entries in `docs/semantic-audit-results.tsv` dated the same day) in the same shared working tree, so deploys had to be isolated via a separate clone rather than risk shipping their unfinished work — that homepage-redesign work was never touched and remains uncommitted/in-progress for whoever owns it. (6) A final Codex-run independent verification pass (not just self-review) caught one real remaining bug: the training page's FAQPage JSON-LD (`SERVICE_FAQS` in `src/app/services/[slug]/page.tsx`) still said £3,500 for the six-week programme after the visible copy had been corrected to £2,500 — fixed and reverified live. All 9 pages confirmed live, 0 em/en dashes across all new content, git history clean. Commits: `4b4a79d`..`98e7854` (attribution/internal-linking, see Sep 3 entry), `29e90c6` (8-page batch), `f14ecaf` (training page), `0ef8e3c` (FAQ pricing fix).
- **Sep 3** — Attribution fix + organic growth diagnosis. (1) Jepto anomaly alert (Aug 31, "0 clicks vs 1 expected, 500%") investigated and codex-cross-checked: statistical noise, not signal (Poisson: 0 happens ~37% of the time at that rate). Real signal was a position slide 29.1->33.6 avg (Aug 25-31, partial recovery to 29.0 by Sep 2); `codex exec` audited git log for that window and found no deploy/config cause (`next.config`, robots, middleware, redirects all untouched) — likely algo/SERP volatility, not self-inflicted. (2) Investigated a perceived "more leads lately" — GA4 showed a real Aug31/Sep1 session spike (36, 35 sessions vs ~15/day baseline) but it was mostly bot noise: 32 of the Aug31 sessions were one-hit-each across ~18 different `/blog/tag/*` archive pages, zero referrer, codex-confirmed as a crawler sweep pattern, not humans. The 4 real `generate_lead` events Sep1-2 traced via `sessionSource`/`sessionMedium` to **google/organic** (`/services/google-algorithm-update-recovery/` x2, `/` x2), not the AI Assistant channel as first suspected. (3) Shipped `src/lib/attribution.ts` (`e29077` era commit — see git log) + wired into `analytics-events.tsx` and `use-lead-form.ts`: first-touch UTM/referrer/landing-page capture in `sessionStorage`, sent with every lead form submit, surfaced as a "Traffic source" line in the enquiry email (`route.ts` `formatSource()`) — previously the email only had the self-reported "how did you hear" dropdown. Live-verified end to end via 2 real test submissions. (4) **Found and fixed a real bug while verifying**: `MAIL_TO` was never set in Vercel prod, so every enquiry was silently going to the code fallback `2012.infinite@gmail.com` instead of `sunnypat81@gmail.com` — Sunny caught it live off the first test email ("all enquiries should come to sunnypat81"). Added `MAIL_TO=sunnypat81@gmail.com` to Vercel prod env + redeployed, re-verified with a second test lead. (5) Pulled real Ahrefs + 90d GSC data for an organic-growth roadmap (no code changes yet, diagnosis only): DR36/524 refdomains is a healthy authority base, but Ahrefs GB only tracks **8** organic keywords (~20 est. monthly traffic) — a relevance/page-quality problem, not authority or indexing (codex cross-check agreed independently). GSC shows ~2000 impr/day across far more queries but stuck avg pos 29-36. Root cause: the `/services/[slug]/` city+service pages fragment identical local intent across too many near-duplicate URLs (doorway-page pattern) — concrete data: Reading/Berkshire cluster (`seo-consultant-reading` 19,184 impr pos22 + `seo-berkshire` 15,364 impr pos20 + `seo-maidenhead`/`seo-wokingham`/`seo-windsor`/`seo-slough`, ~37k impr combined, none past pos15) should consolidate into one hub; `best-seo-companies-uk` (17,742 impr, pos 64.7, competing against Clutch/DesignRush) is a lost cause vs `top-geo-agencies` (7,187 impr, pos 20, actually gaining clicks) which should get pushed instead; `/website-design/packages/`, `/redesign/`, `/small-business/` fragment one service across 3 pages (note: Ahrefs claimed `website-design/packages` ranks #1 for "website design packages uk" — **GSC's real avg position is 55.6, Ahrefs number is unreliable/stale here, don't trust it**). Dead-weight city pages (`seo-consultant-kent/oxford/southampton`) get 2-421 impr each at pos 30-60+ and should be pruned — note these may be the same "18 remote city pages" noindexed Mar 26; if they're showing GSC impressions again either the noindex was reverted or new ones were added since, worth checking before the next content pass. Winners to build more of: `/blog/ai-search-statistics/` (pos 12.9, 25 clicks, best page on the site) and `/tools/schema-generator/` (pos 17.1, 15 clicks) — original-data posts and free tools are the only content types actually converting impressions to clicks right now. **Next action queued, not yet started:** consolidate the Berkshire/Reading and website-design clusters into single deep pages with 301s from the fragments.
- **Aug 26 (session 2, separate from the five-part session below)** — AI-visibility proof content + distribution, all 5 deploys DEPLOYED + live-verified (curl on all 8 touched URLs, then a Playwright console-error + screenshot pass once the Playwright MCP's `mkdir 'G:'` bug forced a local `npm install playwright` workaround, see `reference_headless-screenshot` in the central vault). (1) Built `src/components/ai-visibility-proof.tsx`: 6 sector-labelled AI-referral case-study cards from a fresh live GA4 90-day pull across the portfolio (cross-checked against GSC), inserted into `/ai-visibility/`, `/ai-visibility-consultant/`, `/geo-agency/`, which all made an "I run this on my own portfolio" claim with zero evidence until now (`063ee37`, `c91685f`). (2) Got a Fable-model agent to direct where this should go for actual inbound leads (not just sit on-site); its plan: on-site placement (approved, done below), plus LinkedIn/Reddit/reviews/pub-pitch distribution (NOT approved, deferred, needs a fresh go-ahead before anything external gets posted). (3) **Completed the stale-CTA sweep the Aug 26 five-part session below explicitly left incomplete**: ~40 files still said "Book a free 30-minute call/consultation" (Trafft live-calendar booking was removed Aug 20, promise was already broken) — reworded to "Request a free..." across `service-inline-form.tsx` (the actual enquiry-form submit button, previously said "Book Free Consultation" on literally every service page site-wide), `content-page.tsx`, `sections/cta.tsx`, `exit-intent.tsx`, `blog-sticky-cta.tsx`, `CitationChecklist.tsx`, `analytics-events.tsx` (click-tracker strings updated to match), and ~25 `.mdoc` files (`e306d00`). Deliberately left "Book the £495/£1,500 Audit" wording alone, that is a purchase decision not a broken promise. **Found, NOT fixed, needs its own pass**: the `{% cta %}` / `{% call-to-action %}` Markdoc shortcodes used in 9 service pages (seo-consultant-devon, woocommerce-seo-consultant, affordable-seo-consultant, small-business-seo-consultant, real-estate-seo-consultant, seo-consultant-edinburgh, seo-consultant-leeds, shopify-seo-consultant, magento-seo-consultant) aren't registered in `src/lib/render-markdoc.tsx` (bare `Markdoc.transform`, no custom tag schema) and silently render nothing, curl-confirmed on 2 of them — those 9 pages currently have zero bottom CTA. (4) New `/ai-visibility-results/` page (Dataset + FAQ schema), wired into sitemap.ts, llms.txt, llms-full.txt, the homepage (new `ai-stat-bar.tsx`, "3,948 AI-assistant sessions across 6 sites/90d"), `/about/` (new `about-ai-proof.tsx`, full 6-card grid — per Fable, `/about/` is the one page with evidence it converts AI-referred visitors), and internal links from the 3 highest-AI-traffic blog posts (`37b27b1`). (5) Refreshed `/blog/ai-referral-traffic-study/`: re-pulled all 12 original sites fresh (28 May-26 Aug window vs the original 4 May-1 Aug), new total 4,717 AI sessions (was 5,240), ChatGPT still ~90%; new finding the first pull couldn't show, 2 sites lost most Google organic clicks to a core update in this window and their AI referral sessions did not fall with them; reported honestly that this site's own AI sessions nearly doubled (28→48) while the fitness site in the same dataset dropped to 0 (`aff58bb`). Semantic-audit run on every new/changed prose block (proof cards, results page, blog refresh), comma splices and one contrastive-framing sentence found and fixed each time before shipping. Bing-resubmitted every changed/new URL across 3 batches; Google's sitemap-ping endpoint is deprecated, relying on normal sitemap-lastmod crawling there.
- **Aug 26** — Five-part session, all DEPLOYED + live-verified (curl on production, not just build). (1) Lead-magnet automation (`48779da`, `2a7014f`): the "Free SEO Checklist" blog lead-magnet form only notified Sunny of a lead, never sent the checklist itself — no such asset even existed. Fable drafted a real 47-point checklist (`docs/lead-magnets/seo-audit-checklist.md`, semantic-audit 93/100), built into a branded PDF via Chrome headless `--print-to-pdf` (not the unused `puppeteer` dep) at `public/downloads/seo-audit-checklist.pdf`, `route.ts` now sends it automatically to the lead via a `LEAD_MAGNETS` map, from `hello@sunnypatel.co.uk` (not `forms@`, so replies land right) after Sunny caught the wrong From address live. (2) Sitewide stale-CTA sweep (`fbda137`, extended `97fc553`): Trafft booking was removed from the site in an earlier session but "book a call/consultation" wording survived across ~29+2 files; swapped for enquiry-form language, rewrote 3 sentences in `services/[slug]/page.tsx` that falsely promised live-call specifics ("I'll pull up your site live") since no call exists any more. **Known incomplete: a broader grep after the fact found ~50 MORE instances across ~35 files still saying "Book a free consultation/call/audit"** (mostly `src/content/services/*.mdoc`, plus `for/[slug]/page.tsx`, `service-inline-form.tsx`, `CitationChecklist.tsx`, `ServiceMiniCta.tsx`) — NOT fixed, needs its own pass, the "30-minute" variant wasn't in the original regex. Also fixed the navbar logo gap ("Sunny Patel" had unwanted space because a flex+gap container was splitting the wordmark's text node and span into separate flex items). (3) Content-decay rewrite (`c9b5df0`, remediated `b1fee9d`): GSC showed 10 blog articles losing 80-98% impressions over 3 months (5 confirmed "Crawled - currently not indexed" via the `2012infinite` GSC account — `sunnypat81` 403s on this property, doesn't own it) while sitewide impressions grew ~3.4x, isolating this to specific decaying content not a technical/algo hit. 10 parallel agents did fan-out research + rewrote with real 2026 data (Reddit API blocked network-wide all session, HTTP 403 unauthenticated — substituted UK Business Forums/localsearchforum.com/trade press instead of fabricating quotes). A `codex exec -s read-only` second-opinion review caught real problems the self-audit missed: 2 fabricated Sunny-attributed case-study stats in `google-penalty-recovery` (fixed), a genuine cross-file contradiction (one article claimed "15-30% CTR" as real portfolio data, directly contradicting the site's own real 5.96% published figure — aligned), plus technical-accuracy fixes (GSC "Discovered" vs "Crawled" not-indexed conflation, disavow advice broader than Google's actual guidance, a misapplied ecommerce study cited as if it measured local rankings). Articles: seo-statistics-uk, local-seo-statistics, content-marketing-statistics, freelance-seo-consultant-uk, google-penalty-recovery, what-is-eeat-seo, how-to-build-topical-authority, seo-metrics-for-boards, optimise-content-for-ai-search, value-website-organic-traffic. (4) Lead-gen data pull: real GA4 (property `409078193`, last 90d) shows 40 total `generate_lead` events — google/organic 50%, direct 22.5%, themanifest.com referral 17.5%, chatgpt.com/ai-assistant 5% (2 leads, one is the real Energy Trust inbound lead — see central vault `project_energy-trust-lead.md`). Investigated the Manifest 100%-conversion claim per Sunny's ask: all 7 sessions are identical (desktop/Chrome/**India**/2 pageviews/near-periodic ~11-17 days) — almost certainly a bot, not real leads, flagged not to invest there. Both AI-attributed leads landed on `/about/` specifically (verification-visit pattern), not a service page. (5) Two Fable-directed lead-gen fixes shipped (`97fc553`): added a required "How did you hear about me?" dropdown to the main `/contact/` form (`contact.tsx` + new `FormSelect` in `form-field.tsx`), flows into the owner-notification email and the `generate_lead` GA4 event as `how_heard` — 9/40 leads were unattributed "direct" and AI users often retype URLs rather than click through, so AI influence is likely undercounted; and added a "See the real data" proof-links block to `about-story.tsx` (links to `/portfolio/`, the 120K Bing Copilot citation study, the AI referral traffic study) since that's where verification-visit readers actually are. Full lead-gen strategy + Fable's prioritised P1/P2/P3 plan is in the central vault `project_inbound-lead-gen-strategy.md`, not repeated here.
- **Aug 19-20** — Trafft busy-sync investigated and abandoned. Confirmed (Aug 19, browser javascript_tool, screenshot/read_page still broken machine-wide so drove the DOM directly): the bookable employee (Sunny Patel, `sunnypat81@gmail.com`) has its own Google Calendar connection exposing Family/iPhone/Work; the earlier "Sunny(Figment) Patel" employee record was a dead-end decoy (separate unused employee, not a calendar sync) — Sunny deleted it himself, along with an unrelated "Sunny (Work) Patel" record, after two automation attempts to do it for him were correctly classifier-blocked (destructive UI click without prior yes; a proposed temporary Trafft-API delete-endpoint in the worker, never applied). Also shipped that day: 4 of 5 Trafft dashboard items done via direct DOM manipulation (24h min booking notice; "How did you find me?" required field with ChatGPT/Claude/Google search/Bing-Copilot/Recommendation/Other, scoped to all services; notification-template merge tag `Found via: {{custom_field_1}}` added to the real Appointment-Approved/To-Employee template; 3 test customers cleaned up), all reload-verified. Aug 20: Sunny clarified the real constraint — Figment's calendar cannot be shared to any personal account, full stop, so no automated busy-sync is possible (confirmed Trafft's employee Google Calendar field is single-account OAuth only, no multi-account, no iCal input; only zero-sharing option is manual per-date "Special Days"). Sunny decided: **remove call booking from the site entirely.** Shipped same day, `ba15347`, DEPLOYED + verified live: deleted `about-booking-cta.tsx` and its usage on `/about/`; removed the "Pick a time" success-screen CTA and all `bookingUrl`/`TRAFFT_BOOKING_URL` plumbing from `form-field.tsx`, `use-lead-form.ts`, `service-inline-form.tsx`, `contact.tsx`, `route.ts` (zero references remain, grep-confirmed). Enquiry-email capture (EmailIt) and the intake-worker/Hermes fire-and-forget lead notification are unchanged, still gated on `AAA_INTAKE_ENABLED=1` (still set in Vercel prod). Trafft account/employee/dashboard settings are untouched, only the site's link to it is gone — revisit later whether to keep the Trafft account at all.
- **Aug 18** — AI lead-growth sprint. Task 4 content refresh (`f068853`, DEPLOYED + verified live by independent curl): best-seo-companies-uk refreshed, semantic-audit 88/100, `2026-08-18` dateModified live, H1 "Best SEO Companies UK: 16 Top Agencies Compared (2026)", comparison table rendering; 754 GSC queries pulled, dominant phrasing already matched the existing title so no slug/title rename; all 16 listed agencies re-verified, stale facts fixed (dead domain links for Rise at Seven and Greenpark, Aira moved to Milton Keynes, The SEO Works domain change, Builtvisible acquired by Brave Bison 2025, Distinctly shifted to B2B plus B2C, unverifiable NOVOS client-count claim softened to named clients); added the anonymised flooring-retailer AI-citation proof point + 2 new FAQs; resubmitted to GSC and Bing, both succeeded. Task 2 booking CTA (`3d77301`, DEPLOYED + verified live): `/about/` shows H2 "Book a free 15 minute SEO audit call" linking to `https://sunnypatel.trafft.com/booking?service=6` (target="_blank", rel="noopener", existing gradient-button class); `route.ts` now awaits the intake worker behind `AbortSignal.timeout(4000)` in a try/catch and ALWAYS returns `{ success: true, bookingUrl }`, defaulting to the hardcoded Trafft URL if the worker call fails; `useLeadForm` returns `bookingUrl`, shared `FormSuccess` renders the CTA when set; GA4 `generate_lead` untouched. **Trafft flow changed: auto-booking removed, now customer-driven via the booking URL** (customer picks their own slot on Trafft, nothing is auto-booked server-side). Task 5: LinkedIn post drafted at `G:\My Drive\SEO\sites\sunnypatel.co.uk\linkedin-ai-study-post.md` (794 chars), nothing posted or scheduled. Task 1 worker fix (commit `6b021f8db45326a15dfa604fb9a9601f11be1eac` in the local-only repo `C:\Users\sunny\repos\aaa-intake-worker`, no remote, nothing pushed): code is correct (worker now only creates the Trafft customer record and returns `booking_url`, auto-booking removed), committed but not yet deployed due to a Cloudflare account access issue. Task 3 Trafft dashboard config BLOCKED: Chrome was running and the Trafft admin session was logged in, but claude-in-chrome cannot inject scripts on this machine at all (script injection timeouts, reproduced on sunnypatel.co.uk too, machine-wide, not Trafft-specific); nothing in the Trafft dashboard was changed. FOLLOW-UP SAME DAY (`afd7bd7`, `e90e0b7`, both DEPLOYED + verified): the 2 site-wide em dashes in shared chrome (JSON-LD person caption in `src/lib/schema.ts` x2, tools blurb in `src/components/related-services.tsx`) are fixed; the "Only UK consultant with documented AI-search lead conversion" absolute in all 3 listicle schema files (`schema-best-seo-companies-uk.ts`, `schema-best-aeo-agencies.ts`, `schema-top-geo-agencies.ts`) is qualified to "the only consultant on this list ... (Express Medicals, via Bing Copilot citation)"; `docs/superpowers/` added to `.gitignore` (plan docs carry account IDs, must never reach this public repo). KILL SWITCH: the intake-worker call in `route.ts` is now gated on `AAA_INTAKE_ENABLED === "1"` (env var deliberately NOT set in Vercel), because the worker version still live auto-books a Trafft slot on every lead. The enquiry email is sent by `route.ts` via EmailIt BEFORE the worker call, so gating costs only the Trafft CRM record and the Hermes webhook, not lead capture. **After deploying the fixed worker, set `AAA_INTAKE_ENABLED=1` in Vercel prod to re-enable.** Worker repo now has a private remote: `sunnyp81/aaa-intake-worker`. Em-dash debt beyond chrome: 2,565 instances across ~100 files, nearly all in published content prose, needs a per-sentence judgment sweep, NOT a global replace. Open decision: commit `9975f83` published CF account IDs, vault filenames holding a live token, and a live-weakness disclosure to this PUBLIC repo before `39c8b01` scrubbed the file; history still carries it; the rewrite was attempted and blocked by the permission classifier, so it needs Sunny (local backup tag `backup-pre-scrub-20260818` = `e90e0b7` exists).
- **Aug 2** — Demand-map widen-the-net ship (`b2b741e`, DEPLOYED + verified live + screenshot-checked): shipped the 3 WIP AI-visibility pages (/geo-agency/, /ai-visibility-consultant/, /is-your-brand-visible-in-ai-search/) + sitemap (incl. missing /ai-visibility/ entry) + contextual links; 9 CTR/query realignments OUTSIDE the Jul-19 measurement window (seo-consulting retargeted to "seo strategy consultant" 3.3k-impr cluster, charge-uk truncated metaTitle fixed, woo expert-first, best-aeo-agencies +AEO-thought-leaders section/FAQ for 664-impr emerging queries, llm-optimisation, freelance, website-grader +"seo page grader", packages/keyword-scraper dash fixes); ServiceInlineForm lead capture on keyword-scraper + schema-generator; NEW /blog/ai-referral-traffic-study/ (original 90d GA4 data: 5,240 AI sessions, 12 sites, ChatGPT 91%, site itself only 28 — semantic audit 88/100); stale "Q2 2026 slots" claim removed from ai-search-optimisation. 7 URLs Bing-submitted. Demand context: GSC impr tripled QoQ to 142k with 44 clicks; GEO/AEO cluster 197→6,129 impr QoQ. Full demand map: `G:\My Drive\Claude Code Work\Demand_Maps\sunnypatel.co.uk_demand_map.md`. Evolve log has the out-of-band entry; measure ~Sep 1. Untouched (frozen): homepage ("technical seo agency" pos 2.6 opportunity, wait for ~Aug 16 verdict), Reading/Berkshire ring, technical-seo-audit, it7/it11 targets.
- **Jul 10** — evolve-site iteration 2 (`d663b08`/`ea49512`): new `/blog/chatgpt-prompts-for-seo/` de-thins/de-orphans `/tools/seo-prompts/` (weakest-linked of the 3 Jun14-15 gap-page tools), reciprocal link added back from the tool. Fixed stale "22 prompts" claim on the tool page (actual count 20). Pushed to `origin/master`; **Vercel deploy still needs Sunny's manual `npx vercel --prod`** (no stored Vercel CLI credentials on this VPS, OAuth device flow can't complete non-interactively). Two more tool-supporting posts queued (ROI explainer, how-to-add-schema, latter needs an angle distinct from `/blog/seo-semantic-markup-guide/` to avoid cannibalisation). `best-seo-companies-uk` refresh (12.5k impr, pos 68) still untouched.
- **Jul 7** — evolve-site loop started: CTR title/meta rewrites on 5 striking-distance pages (`b55cf1c`), loop state in `docs/evolve/`. Shipped the pending homepage refresh WIP (`981b004`): new hero copy, Geist heading font, smaller H1; consolidated /services/ai-visibility-audit/ into /services/paid-seo-audit/ with 301. Verdict pass due Jul 14-21.
- **Jun 11** — `/cv` now serves `public/cv.pdf` via `next.config.ts` rewrite (`/cv`+`/cv/` -> `/cv.pdf`, 308 via trailingSlash). Commit `6ef0f53`.
- **Jun 8-10** — 2 Trafft "Free SEO Audit" leads (details in Trafft dashboard only). Built/GEO-optimised `/blog/best-aeo-agencies/` (renamed from `-uk-2026`, 301'd): 2.7k words, ItemList (8 orgs) + FAQPage + SpeakableSpecification, semantic audit 92/100, 4 inbound links, live + Bing-submitted, commit `b88ee02`. Hardened GA4 `generate_lead` in `src/lib/use-lead-form.ts`.
- **Jun 15** — Claude Design conversion-redesign prompt drafted (light mode, Bricolage/Hanken, blue, SP monogram; home as primary conversion page, 3 offers, £495 audit product page, sticky CTA, named-testimonial proof, WCAG 2.1 AA). Not yet built.
- **May 10** — Reading cannibalization fix + CTR rewrites (`d814409`): homepage was outranking `/services/seo-consultant-reading/` (pos 3.9 vs 37) for the money query; stripped "Reading/Berkshire" from homepage title/desc/hero anchor, rewrote Reading + Berkshire service titles, added local pill links.
- **May 6-7** — Shipped `/website-design/` Phase 1: 14 pages (ROOT + 6 NODE hubs + 7 SEEDs) + 28 inline SVGs (`01c79b7`, `16c0a05`). New `websiteDesign` Keystatic collection + catch-all route. Pricing floor £1,500. Phase 2-4 + homepage cross-link outstanding.
- **Apr 16** — CTR rewrites on homepage + 5 blog posts (`dd874a6`).
- **Apr 14** — GEO/AEO audit (was 52/100): 301 Berkshire URL, dateModified + Speakable on service schema, Clutch sameAs, ghost-citation fixes, new `/blog/seo-consultant-vs-agency/` + `/services/local-seo-reading/`. Pending: Clutch reviews, GBP/Bark/Yell/DesignRush, Wikidata.
- **Apr 13** — GSC audit (33 clicks / 24.3k impr / 0.14% CTR): CTR rewrites on 4 top-impression pages (`a2e96c8`), IndexNow wired (key file added), dynamic OG image via next/og (`f31d1aa`). Note: `@sunnypat81` twitter handle unconfirmed.
- **Apr 9** — Built + deployed `/tools/` hub: 20 free SEO tools + 5 API routes (redirect/og/ssl/grade/links). Website Grader = lead funnel to `/contact/`.
- **Apr 4** — Contact-page cannibalization fix (`ac798bb`): removed "Reading, Berkshire" from contact, excluded canonical'd pages from sitemap.
- **Mar 28** — Crawl/CTR sprint (`aed71ae`, `91039f3`, `f5942ca`): removed mid-content CTAs, canonical consolidation, fixed 30 non-trailing-slash links.
- **Mar 26** — Berkshire refresh + fixed 872 trailing-slash internal links (root cause of dual indexing); noindexed 62+ tag pages + 18 remote city pages; rewrote llms.txt.
- **Mar 17-19** — Trailing-slash root cause found (Mar 14 config flip without sitemap update split all signals); sitemap + internal links + schema geo-signals fixed; OG image filename de-leaked (`3acf3dc`); best-seo-companies-uk expanded 8->11 agencies; citation audit (53 bulk citations low quality; Chamber/Clutch/GBP worth more than all of them).
