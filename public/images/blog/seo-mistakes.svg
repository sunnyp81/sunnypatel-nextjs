<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
<defs>
    <!-- Noise texture -->
    <filter id="noise" x="0" y="0" width="100%" height="100%">
      <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch"/>
      <feColorMatrix type="saturate" values="0"/>
    </filter>
    <!-- Soft blur for atmosphere -->
    <filter id="blur-lg"><feGaussianBlur stdDeviation="60"/></filter>
    <filter id="blur-md"><feGaussianBlur stdDeviation="35"/></filter>
    <filter id="blur-sm"><feGaussianBlur stdDeviation="18"/></filter>
    <!-- Glow filter -->
    <filter id="glow">
      <feGaussianBlur stdDeviation="6" result="blur"/>
      <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
    <filter id="glow-strong">
      <feGaussianBlur stdDeviation="12" result="blur"/>
      <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
    </filter>
    
  </defs>
<!-- Background -->
<rect width="1200" height="630" fill="#050507"/>

  <ellipse cx="600" cy="320" rx="250" ry="200" fill="#d79f1e" opacity="0.04" filter="url(#blur-lg)"/>
  <!-- Triangle — clean lines, gradient stroke feel -->
  <path d="M600,180 L760,430 L440,430 Z" fill="none" stroke="#d79f1e" stroke-width="2" opacity="0.3" filter="url(#glow)"/>
  <!-- Inner glow of triangle -->
  <path d="M600,210 L740,415 L460,415 Z" fill="#d79f1e" opacity="0.02"/>
  <!-- Exclamation -->
  <line x1="600" y1="270" x2="600" y2="360" stroke="#d79f1e" stroke-width="3" stroke-linecap="round" opacity="0.4" filter="url(#glow)"/>
  <circle cx="600" cy="390" r="4" fill="#d79f1e" opacity="0.4" filter="url(#glow)"/>

<!-- Noise overlay -->
<rect width="1200" height="630" filter="url(#noise)" opacity="0.03"/>
</svg>